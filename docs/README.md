# docs

仕様/設計メモ、ユースケース、図などのドキュメントを管理します。

## 構成
- `api-fastapi`: FastAPI関連のドキュメント（ユースケース、ER、用語集など）
- `crm-rails-api`: Rails API関連のドキュメント

## 使い方
- `.md` はそのまま閲覧します。
- `.pu` は PlantUML で図を生成できます。
  例: `plantuml docs/api-fastapi/*.pu`
